{
  "name": "clothing-line-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose up -d --wait && node scripts/prepare-env.mjs && turbo run dev --ui=tui",
    "stop": "docker compose down",
    "setup": "node scripts/prepare-env.mjs",
    "clean": "docker compose down -v"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.18.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "turbo": "^2.10.8"
  }
}
